{% extends "base.html.twig" %}
{% block body %}
        {{ form_start(form) }}
        {{ form_widget(form) }}
        <button type="submit" class="btn btn-warning mt-2" style="margin-left: 50px">Add category</button>
        {{ form_end(form) }}
    {% endblock %}